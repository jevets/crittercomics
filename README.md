Statically generated site with Eleventy and Sanity.io.

1. Clone the repo
2. `yarn install` (or `npm install`)

- `yarn dev` for local development: watches files, refreshes browser
- `yarn build` for production build
